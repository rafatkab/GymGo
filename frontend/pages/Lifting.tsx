import { View, Text, SafeAreaView } from "react-native";
import React from "react";

const Lifting = () => {
  return (
    <SafeAreaView className="flex-1 items-center justify-center bg-white">
      <Text className="text-3xl">LiftingPage</Text>
    </SafeAreaView>
  );
};

export default Lifting;
